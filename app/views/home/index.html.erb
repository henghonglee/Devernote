<% if current_user %>
	<% if current_user.github_username %>
		<h1><%= current_user.github_username%></h1><a href="#">change</a>

	<% end %>
	<%for repo in current_user.repos%>
		<h2>Repo name : <%=repo.name%><h2>
		<h4>Repo clone_url : <%=repo.clone_url%></he>
		<h4>Repo hook_url : <%=root_url%>repo/<%= repo.id %>?user_id=<%=current_user.id%>  <a href="/repo/<%= repo.id %>?user_id=<%=current_user.id%>">update notes now</a><h4>			
	<%end%>
<%else%>
<%end%>